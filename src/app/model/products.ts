import { FormGroup, FormControl, Validators } from '@angular/forms';
export class Product {


    constructor(
        id: number,
        title: string,
        price: number,
        category: string,
        description: string,
        image: string,
    ) { }
}

  